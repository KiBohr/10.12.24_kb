
function math(para1, para2){
    let multi = para1*para2;
    console.log(`Mulitplikation von ${para1} und ${para2}: ${multi}`);

    let divi = para1 / para2;
    console.log(`Division von ${para1} und ${para2}: ${divi}`);
    
}
math(1,1000)